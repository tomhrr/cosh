#!/bin/sh
# This wrapper script exists so that abort-type error messages can be
# seen before the window closes, in situations where the shell is not
# being run within some other shell.
/usr/local/bin/_cosh;
echo "Press enter to continue";
read UNUSED;
